name: Fourth WorkFlow
on:
  workflow_run:
    workflows: [Third WorkFlow]
    types:
      - completed

jobs:
  run-command-one:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello
        run: echo "Hello $GITHUB_ACTOR"